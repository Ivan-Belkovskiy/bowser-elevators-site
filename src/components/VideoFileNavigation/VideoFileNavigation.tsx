'use client';

import ModalWrapper from "../ModalWrapper/ModalWrapper";

export default function VideoFileNavigation() {
    return (
        <ModalWrapper>
            <div className="video-file-navigation"></div>
        </ModalWrapper>
    );
}